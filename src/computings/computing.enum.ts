export enum ComputingQueueStatus {
    PENDING = 'pending',
    PROCESSING = 'processing',
    COMPLETED = 'completed',
    CANCELED = 'canceled',
}

export enum ComputingUserType {
    TEACHER = 'teacher',
    POSTGRADUATE = 'postgraduate',
    OTHER = 'other'
}