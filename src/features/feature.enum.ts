export enum FeatureCode {
    STORAGE = 'storage',
    COMPUTING = 'computing',
    VIRTUAL_MACHINE = 'virtual-machine'
}
export enum FeatureStatus {
    ACTIVE = 'active',
    DEACTIVE = 'deactive',
}